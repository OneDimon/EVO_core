# n8n/ — Immune System workflow (часть Блока 07)

**Перед правкой обязательно прочитать:** `../BLOCK_07_immune_system.md`,
`README_n8n.md` (в этой же папке).

## Что здесь
- `evo_immune_system_workflow.json` — экспортированный n8n-workflow иммунной
  системы (реаниматор/патчи, см. Блок 07 в `core/immune_system.py`).
- `README_n8n.md` — инструкция по импорту/настройке.

## ⚠️ Статус на момент аудита
Workflow существует как файл, но **сервис n8n не добавлен в
`../docker-compose.yml`** — интеграция не задеплоена как часть стека.
Проверить актуальность этого статуса перед тем, как на него полагаться;
при добавлении сервиса — сверить порты/сеть с существующими
`postgres`/`redis`/`api` сервисами в `docker-compose.yml`.
